@model Blog.Repository.Models.UserModel
@{
    Blog.Repository.Models.UserModel user = Model ?? new Blog.Repository.Models.UserModel
    {
        FullName = "John Doe",
        AvatarUrl = "Default.jpg"
    };
}

<ul class="nav navbar-right">
    <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false">
            @*<img src="@("Content/Avatars/" + (user.AvatarUrl ?? "Default.jpg"))" width="32" height="32" />*@ <strong>@user.FullName</strong><i class="caret"></i>
        </a>
        <ul class="dropdown-menu">
            @*<li>
                <a href="#"><i class="glyphicon glyphicon-user"></i> User Profile</a>
            </li>
            <li class="divider"></li>*@
            <li>
                <a href="@Url.Action("Logout", "Auth", new { ReturnUrl = HttpContext.Current.Request.Url })"><i class="glyphicon glyphicon-off"></i> Sign Out</a>
            </li>
        </ul>
    </li>
</ul>
@model Blog.Repository.Models.UserModel

<ul class="nav navbar-right">
    <li class="dropdown">        
        <a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false">
            <img src="@("Content/Avatars/" + (Model.AvatarUrl ?? "Default.jpg"))" width="32" height="32" />    <strong>@Model.FullName</strong><i class="caret"></i>
        </a>
        <ul class="dropdown-menu">
            <li>
                <a href="#"><i class="glyphicon glyphicon-user"></i> User Profile</a>
            </li>
            <li class="divider"></li>
            <li>
                <a href="@Url.Action("Logout", "Auth")"><i class="glyphicon glyphicon-off"></i> Logout</a>
            </li>
        </ul>
    </li>
</ul>